# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v56.0.0/ before writing any code.

For small UI changes, small refactors, and so on, keep the LLM as lightweight as possible -- don't look up Expo docs.
